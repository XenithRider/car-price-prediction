# -*- coding: utf-8 -*-
"""Car_price_prediction.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1hUkUHj8rw1q9nbOO4AvBcgh9wFM9JZ-H
"""

import pandas as pd

import joblib

from sklearn.ensemble import RandomForestRegressor
from sklearn.model_selection import train_test_split

DATA_PATH = "/content/cardekho_data (1).csv"

MODEL_PATH = "/content/random_forest_model.pkl"
COLS_PATH = "/content/feature_columns.pkl"

df = pd.read_csv(DATA_PATH)

df.head()

df.info()

# Target / Features
X = df.drop("Selling_Price", axis=1)
y = df["Selling_Price"]

# One-hot encode categorical columns (same idea as your notebook)
categorical_cols = ["Fuel_Type", "Seller_Type", "Transmission", "Owner", "Car_Name"]
X_encoded = pd.get_dummies(X, columns=categorical_cols, drop_first=True)

# Split
X_train, X_test, y_train, y_test = train_test_split(
X_encoded, y, test_size=0.2, random_state=42
    )

#Train model
model = RandomForestRegressor(n_estimators=500, random_state=42, n_jobs=-1)
model.fit(X_train, y_train)

# Save model + training columns (VERY IMPORTANT for inference alignment)
joblib.dump(model, MODEL_PATH)
joblib.dump(list(X_train.columns), COLS_PATH)

print("Model saved to:", MODEL_PATH)
print("Feature columns saved to:", COLS_PATH)
print("Train columns count:", len(X_train.columns))

# Evaluate the model
r_squared = model.score(X_test, y_test)
print(f"Model R-squared score on the test set: {r_squared:.4f}")

